# ============================================================
#  Environment configuration – STAGING
#  File loaded automatically when -Denv=staging
# ============================================================

# Base URL of the application under test
# TODO: Replace with the real staging URL when available
base.url=https://staging.cloud.google.com

# Default browser timeout (milliseconds)
default.timeout=30000

# Slow-motion delay between actions (ms) – set 0 to disable
slow.motion=0

# Viewport dimensions
viewport.width=1920
viewport.height=1080
